// Write a program in C that prints the perimeter of a rectangle to take its height and width
// perimeter = 2 * (width + height)

#include <stdio.h>
 int main(){
int width height p;
printf("Please add the width");
scanf("%d", &width);
printf("Please add the height");
scanf("%d", &width);
p = 2 * (width + 2)
printf("The permiter is $d", p);

    return 0;
}


